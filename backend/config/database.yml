# SQLite. Versions 3.8.0 and up are supported.
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem "sqlite3"
#
default: &default
  adapter: sqlite3
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000

development:
  <<: *default
  database: storage/development.sqlite3

# MySQL configuration (commented out for SQLite3 development)
#development:
#  adapter: mysql2
#  pool: 10
#  username: sushilover
#  password: uf***
#  database: sushi
#  encoding: utf8
#  socket: /var/run/mysqld/mysqld.sock
#  reconnect: true

# Uncomment below and comment out the above 'development' section to use legacy SUSHI MySQL database
# Also set LEGACY_DATABASE=true environment variable
# development:
#   adapter: mysql2
#   encoding: utf8mb4
#   collation: utf8mb4_general_ci
#   pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
#   database: <%= ENV.fetch("LEGACY_MYSQL_DATABASE", "sushi_db") %>
#   username: <%= ENV.fetch("LEGACY_MYSQL_USER", "root") %>
#   password: <%= ENV.fetch("LEGACY_MYSQL_PASSWORD", "") %>
#   host: <%= ENV.fetch("LEGACY_MYSQL_HOST", "localhost") %>
#   port: <%= ENV.fetch("LEGACY_MYSQL_PORT", 3306) %>

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: storage/test.sqlite3

# Store production database in the storage/ directory, which by default
# is mounted as a persistent Docker volume in config/deploy.yml.
<% if ENV["LEGACY_DATABASE"] == "true" %>
# --- Legacy-coexistence production profile ------------------------------------
# Running in production against the legacy shared SUSHI MySQL `sushi` DB via socket
# (coexisting with legacy production SUSHI). SINGLE primary connection only — no
# cache/queue/cable. The Solid stack is disabled in production.rb under the same
# LEGACY_DATABASE flag, so nothing connects to or creates solid_* tables in the live
# schema. Uses ENV["SUSHI_DB_PASSWORD"] (NOT ENV.fetch without a default: ERB renders
# every env block, so a missing var would KeyError unrelated commands). DO NOT migrate.
production:
  primary:
    adapter: mysql2
    encoding: utf8mb4
    pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
    database: <%= ENV.fetch("SUSHI_DB_NAME", "sushi") %>
    username: <%= ENV.fetch("SUSHI_DB_USERNAME", "sushilover") %>
    password: <%= ENV["SUSHI_DB_PASSWORD"] %>
    socket: /var/run/mysqld/mysqld.sock
    reconnect: true
<% else %>
production:
  primary:
    adapter: mysql2
    encoding: utf8mb4
    pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
    database: <%= ENV.fetch("MYSQL_DATABASE", "sushi_app_production") %>
    username: <%= ENV.fetch("MYSQL_USER", "root") %>
    password: <%= ENV.fetch("MYSQL_PASSWORD", "") %>
    host: <%= ENV.fetch("MYSQL_HOST", "db") %>
    port: <%= ENV.fetch("MYSQL_PORT", 3306) %>
  cache:
    adapter: mysql2
    encoding: utf8mb4
    pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
    database: <%= ENV.fetch("MYSQL_DATABASE", "sushi_app_production_cache") %>
    username: <%= ENV.fetch("MYSQL_USER", "root") %>
    password: <%= ENV.fetch("MYSQL_PASSWORD", "") %>
    host: <%= ENV.fetch("MYSQL_HOST", "db") %>
    port: <%= ENV.fetch("MYSQL_PORT", 3306) %>
    migrations_paths: db/cache_migrate
  queue:
    adapter: mysql2
    encoding: utf8mb4
    pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
    database: <%= ENV.fetch("MYSQL_DATABASE", "sushi_app_production_queue") %>
    username: <%= ENV.fetch("MYSQL_USER", "root") %>
    password: <%= ENV.fetch("MYSQL_PASSWORD", "") %>
    host: <%= ENV.fetch("MYSQL_HOST", "db") %>
    port: <%= ENV.fetch("MYSQL_PORT", 3306) %>
    migrations_paths: db/queue_migrate
  cable:
    adapter: mysql2
    encoding: utf8mb4
    pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
    database: <%= ENV.fetch("MYSQL_DATABASE", "sushi_app_production_cable") %>
    username: <%= ENV.fetch("MYSQL_USER", "root") %>
    password: <%= ENV.fetch("MYSQL_PASSWORD", "") %>
    host: <%= ENV.fetch("MYSQL_HOST", "db") %>
    port: <%= ENV.fetch("MYSQL_PORT", 3306) %>
    migrations_paths: db/cable_migrate
<% end %>
